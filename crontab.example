# ========================================
# CRONTAB - AGENTE PETSHOP
# ========================================
#
# Instalar:
#   crontab -e
#   Copiar as linhas abaixo (ajustar caminhos)
#
# Verificar:
#   crontab -l
#
# ========================================

# Processar lembretes a cada 5 minutos
*/5 * * * * cd /path/to/agentedaauzap && npm run cron:reminders >> /var/log/reminders.log 2>&1

# Limpar cache a cada hora
0 * * * * cd /path/to/agentedaauzap && npm run cache:clear >> /var/log/cache.log 2>&1

# Backup diário às 3h da manhã
0 3 * * * cd /path/to/agentedaauzap && npm run backup:daily >> /var/log/backup.log 2>&1

# Relatório semanal (domingo às 20h)
0 20 * * 0 cd /path/to/agentedaauzap && npm run report:weekly >> /var/log/reports.log 2>&1

# ========================================
# NOTAS
# ========================================
# - Ajustar /path/to/agentedaauzap para o caminho real
# - Criar diretório de logs: mkdir -p /var/log
# - Garantir permissões: chmod +x src/cron/*.ts
# - Verificar NODE_PATH e variáveis de ambiente
# - Monitorar logs regularmente
